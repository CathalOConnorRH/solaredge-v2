# AGENTS.md

This repo's agent/contributor onboarding lives in **[CLAUDE.md](./CLAUDE.md)** —
read it first. It covers the two-repo relationship with `ha-solaredge-one`, the
shared test venv, how to run tests/lint/mypy, the code map, confirmed API facts,
testing conventions, and the Trusted-Publishing release process.
